<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.paging">

        <sql id="pagingQuery">
            LIMIT #{paging.displayRow} OFFSET #{paging.offset}
        </sql>

</mapper>