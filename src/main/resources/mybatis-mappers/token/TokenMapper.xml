<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dejay.framework.mapper.token.TokenMapper">
    <insert id="saveToken">
        INSERT INTO TOKEN(TOKEN, REFRESH_TOKEN, TOKEN_TYPE) VALUES (#{token}, #{refreshToken}, #{tokenType})
    </insert>

</mapper>